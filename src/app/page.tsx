'use client';

// Imports
// React imports
import React from 'react';

// Ant Design imports
import { Button } from 'antd';

const Home = () => (
  <div className="App">
    <Button type="primary">Button</Button>
  </div>
);

export default Home;
